@model IEnumerable<Serviciu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<br />
<br />
<br />
<br />
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Denumire)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descriere)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descriere)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ServiciuID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ServiciuID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ServiciuID })
            </td>
        </tr>
    }

</table>
